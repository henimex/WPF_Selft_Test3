<Window x:Class="ContactsApp.NewContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContactsApp"
        mc:Ignorable="d"
        Title="NewContactWindow" Height="360" Width="300" WindowStartupLocation="CenterScreen"> 
    <Grid>
        <StackPanel Margin="10">
            <Label Content="Name" Margin="5"/>
            <TextBox x:Name="TxtName" Margin="0,0,0,5" FontSize="20"/>
            <Label Content="e-Mail" Margin="5"/>
            <TextBox x:Name="TxtEmail" Margin="0,0,0,5" FontSize="20" />
            <Label Content="Phone Number" Margin="5"/>
            <TextBox x:Name="TxtPhone" Margin="0,0,0,5" FontSize="20"/>
            <Button x:Name="BtnSave" Content="Save" Click="BtnSave_OnClick" FontSize="15" FontWeight="DemiBold" Margin="2,10" Padding="5"/>
        </StackPanel>
    </Grid>
</Window>
